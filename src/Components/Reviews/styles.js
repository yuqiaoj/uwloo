import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  reviews: {
    maxHeight: "30vh",
    overflow: "auto",
  },
  reviewItem: {
    margin: "10px 0px",
  },
}));
